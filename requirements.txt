# Runtime dependencies for the API that main.py actually serves.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
requests>=2.31.0
psutil>=5.9.0
websockets>=12.0
python-multipart>=0.0.9

# Only needed by the alternate Gemini clients (gemini_client_simple/fixed/new.py)
# and api/appointment_calendar.py, none of which main.py currently imports.
# Uncomment if you wire them up:
# google-generativeai>=0.5.0
# google-api-python-client>=2.120.0
# google-auth>=2.28.0
